import('./err.js')

const obj = {
  hello: "world",
  bool: true,
  nested: {
    nested2: {
      nested3: {
        nested4: {
          message: "hello world",
        }
      }
    }
  }
}

console.log("Hello world", 2323, new Date());
console.log(obj);
